# Split history playground

The playground is an executable routing specification. It exposes deterministic pages and controls for:

- pane-local `push` and `replace`
- redirect and guard-abort outcomes
- exact browser back and forward restoration
- split direction and single-pane presentation
- bounded inactive-page retention and Vue lifecycle counters

Run it from the repository root with `pnpm play`.
